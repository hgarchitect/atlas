# AWS Standard

Treat infrastructure intent and deployed runtime as separate evidence classes. IaC/configuration describes intended state; direct AWS inventory, task/image revisions, logs and telemetry establish deployed conformance.

Prefer least privilege, explicit ownership, observable failure modes, rollback paths and cost awareness. Never store credentials in Atlas.
